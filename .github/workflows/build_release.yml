name: Build release
permissions:
  contents: write
  actions: read
  pull-requests: write

on:
  workflow_dispatch:
    inputs:
      release_type:
        description: "Release type"
        required: false
        type: choice
        default: Nightly
        options:
          - Nightly
          - Stable
          - Both
      experimental:
        description: "Build with experimental flag"
        required: false
        type: boolean
        default: false
      skip_translation_progress:
        description: "Skip translation progress chart update"
        required: false
        type: boolean
        default: true
  push:
    paths:
      - "source/**"
      - "tests/**"

jobs:
  check_localization:
    runs-on: windows-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Check localization keys
        run: python scripts/check_localization.py

  lint:
    runs-on: windows-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Setup .NET
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: 10.0.x

      - name: Restore dependencies
        run: dotnet restore DualSenseClient.sln

      - name: Fetch native libraries
        run: python scripts/fetch_native_libraries.py win-x64

      - name: Lint and verify formatting is clean
        run: python scripts/lint.py --install --pin-version --check

      - name: Test
        run: dotnet test tests/DualSenseClient.Tests/DualSenseClient.Tests.csproj

  update_translation_progress:
    needs: check_localization
    if: |
      needs.check_localization.result == 'success' &&
      github.ref == 'refs/heads/main' &&
      github.event_name == 'workflow_dispatch' &&
      !inputs.skip_translation_progress
    uses: ./.github/workflows/update_translation_progress.yml

  build_nightly:
    needs: [check_localization, lint, update_translation_progress]
    if: |
      always() &&
      needs.check_localization.result == 'success' &&
      needs.lint.result == 'success' &&
      (needs.update_translation_progress.result == 'success' || needs.update_translation_progress.result == 'skipped') &&
      (github.event_name == 'push' || inputs.release_type == 'Nightly' || inputs.release_type == 'Both')
    uses: ./.github/workflows/build_job.yml
    with:
      is_stable: false
      is_experimental: ${{ inputs.experimental || false }}

  build_stable:
    needs: [check_localization, lint, update_translation_progress]
    if: |
      always() &&
      github.event_name == 'workflow_dispatch' &&
      (inputs.release_type == 'Stable' || inputs.release_type == 'Both') &&
      needs.check_localization.result == 'success' &&
      needs.lint.result == 'success' &&
      (needs.update_translation_progress.result == 'success' || needs.update_translation_progress.result == 'skipped')
    uses: ./.github/workflows/build_job.yml
    with:
      is_stable: true

  create_nightly_release:
    needs: build_nightly
    if: |
      always() &&
      github.event_name == 'workflow_dispatch' &&
      (inputs.release_type == 'Nightly' || inputs.release_type == 'Both') &&
      needs.build_nightly.result == 'success' &&
      (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/dev')
    uses: ./.github/workflows/create_release.yml
    with:
      is_stable: false
    secrets: inherit

  create_stable_release:
    needs: build_stable
    if: |
      always() &&
      github.event_name == 'workflow_dispatch' &&
      (inputs.release_type == 'Stable' || inputs.release_type == 'Both') &&
      needs.build_stable.result == 'success' &&
      github.ref == 'refs/heads/main'
    uses: ./.github/workflows/create_release.yml
    with:
      is_stable: true
    secrets: inherit
